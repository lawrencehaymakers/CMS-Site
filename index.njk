---
layout: base.njk
title: Lawrence Haymakers Baseball
permalink: /
---
<style>
  .pillars { display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); gap: 0; }
  .pillar { padding: 2.5rem 2rem; border-right: 1px solid rgba(255,255,255,0.1); text-align: center; }
  .pillar:last-child { border-right: none; }
  .pillar-icon { font-size: 2.2rem; margin-bottom: 1rem; }
  .pillar h3 { font-family: 'Barlow Condensed', sans-serif; font-weight: 900; font-size: 1.25rem; text-transform: uppercase; letter-spacing: 0.05em; color: var(--yellow); margin-bottom: 0.5rem; }
  .pillar p { font-size: 0.9rem; color: rgba(255,255,255,0.72); line-height: 1.6; }
  .mission-band { background: var(--yellow); padding: 3rem 2rem; text-align: center; }
  .mission-band blockquote { font-family: 'Barlow Condensed', sans-serif; font-weight: 900; font-size: clamp(1.5rem, 4vw, 2.4rem); text-transform: uppercase; color: var(--green-dark); max-width: 700px; margin: 0 auto; line-height: 1.15; letter-spacing: 0.01em; }
  .mission-band cite { display: block; font-family: 'Barlow', sans-serif; font-weight: 500; font-size: 0.8rem; letter-spacing: 0.12em; text-transform: uppercase; color: var(--green); margin-top: 1rem; font-style: normal; }
  .cta-strip { background: var(--green-dark); padding: 3.5rem 2rem; text-align: center; }
  .cta-strip h2 { font-family: 'Barlow Condensed', sans-serif; font-weight: 900; font-size: clamp(1.8rem, 4vw, 2.6rem); text-transform: uppercase; color: white; margin-bottom: 0.5rem; }
  .cta-strip p { color: rgba(255,255,255,0.65); margin-bottom: 1.75rem; font-size: 0.95rem; }
  @media (max-width: 680px) { .pillar { border-right: none; border-bottom: 1px solid rgba(255,255,255,0.1); } .pillar:last-child { border-bottom: none; } }
</style>

<section class="hero">
  <div class="hero-eyebrow">Lawrence, Kansas · 10U Baseball</div>
  <h1>{{ home.hero_line1 }}<br><em>{{ home.hero_line2 }}</em><br>{{ home.hero_line3 }}</h1>
  <p class="hero-sub">{{ home.hero_sub }}</p>
  <div class="hero-actions">
    <a href="/tryouts/" class="btn btn-primary">Tryout Info</a>
    <a href="/philosophy/" class="btn btn-outline">Our Philosophy</a>
  </div>
</section>

<div class="dark-band">
  <div class="pillars">
    <div class="pillar">
      <div class="pillar-icon">⚾</div>
      <h3>Development First</h3>
      <p>We track growth, not wins. Every practice and every game is a chance to get better at the fundamentals.</p>
    </div>
    <div class="pillar">
      <div class="pillar-icon">🤝</div>
      <h3>Team Over Everything</h3>
      <p>We win together and we lose together. Effort, attitude, and respect matter more than the score.</p>
    </div>
    <div class="pillar">
      <div class="pillar-icon">🌱</div>
      <h3>Character Counts</h3>
      <p>We're building habits that last long after 10U. How you carry yourself matters as much as how you play.</p>
    </div>
  </div>
</div>

<div class="mission-band">
  <blockquote>"Develop players who love the game, understand teamwork, and carry strong character on and off the field."</blockquote>
  <cite>— Lawrence Haymakers Mission</cite>
</div>

<div class="cta-strip">
  <h2>{{ home.cta_headline }}</h2>
  <p>{{ home.cta_sub }}</p>
  <a href="/tryouts/" class="btn btn-primary">Register for Tryouts</a>
</div>
